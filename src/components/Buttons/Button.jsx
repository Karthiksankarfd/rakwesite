import React from 'react'

const Button = ({name}) => {
  return (
    <button className='w-fit bg-yellow-400 text-black rounded-3xl py-2 px-4 font-semibold'>{name}</button>
  )
}

export default Button

